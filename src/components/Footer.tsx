import { Brain, Github, Linkedin, Mail, Heart } from "lucide-react";
const Footer = () => {
  const currentYear = new Date().getFullYear();
  return <footer className="bg-gradient-to-r from-primary/10 via-background to-accent/10 border-t border-border/50">
      
    </footer>;
};
export default Footer;